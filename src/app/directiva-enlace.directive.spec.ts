import { DirectivaEnlaceDirective } from './directiva-enlace.directive';

describe('DirectivaEnlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaEnlaceDirective();
    expect(directive).toBeTruthy();
  });
});
